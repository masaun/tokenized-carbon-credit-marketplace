{
  "name": "tokenized-carbon-credit-wrapped-NFT-marketplace",
  "version": "1.0.0",
  "description": "Tokenized carbon credit wrapped NFT marketplace",
  "main": "index.js",
  "scripts": {
    "test": "truffle test ./test/*",
    "console": "truffle console --network local",
    "migrate:local": "truffle migrate --reset --network local",
    "migrate:ropsten": "truffle migrate --reset --network ropsten"
  },
  "author": "masaun",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "@truffle/hdwallet-provider": "^1.2.0"
  },
  "devDependencies": {}
}
